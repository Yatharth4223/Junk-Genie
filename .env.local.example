# Server-side only (Next.js API routes)
# Path to your Google Cloud Vision service account key JSON (never commit the JSON).
GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json

# Google GenAI API key (AI Studio / Gemini API key)
GOOGLE_GENAI_API_KEY=replace_me

